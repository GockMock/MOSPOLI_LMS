# Frontend should use same-origin /api by default.
# In development Vite proxies /api to AI_NAVIGATION_UPSTREAM.
AI_NAVIGATION_UPSTREAM=http://localhost:3001

# Leave empty for same-origin requests: fetch('/api/navigation-search').
# Set only if you intentionally want direct browser calls to another origin.
VITE_AI_NAVIGATION_URL=
